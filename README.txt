This module contains function used to get list of prime and check if prime when check attribute is set to True
ptototype:
	primechecker(target,check=False)
pip install primes
then import it.
Have fun.